from setuptools import setup, find_packages

if __name__ == "__main__":
    print(find_packages())
    # setup(
    #     name='bdrp',
    #     version='1.0.0',
    #     packages=find_packages(),
    # )
